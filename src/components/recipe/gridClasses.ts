/**
 * The responsive grid the recipe list lays its cards on.
 *
 * Shared with the route's loading skeleton so the placeholders sit exactly
 * where the real cards will, and nothing jumps when they arrive.
 */
export const RECIPE_GRID_CLASSES =
  'grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-4'
